{
  "compilerOptions": {
    "target": "ES2020", /* Define a versão do JavaScript para a qual o TypeScript será transpilado. */
    "module": "commonjs", /* Define o sistema de módulo que será utilizado. */
    "baseUrl": "./", /* Define o diretório base para resolução de módulos. */
    "paths": {
      "@/*": [
        "src/*"
      ] /* Mapeia o alias '@' para a pasta 'src'. */
    },
    "types": [
      "vitest/globals" /* Inclui os tipos do Vitest globalmente. */
    ],
    "esModuleInterop": true, /* Emite JavaScript adicional para facilitar a importação de módulos CommonJS. */
    "forceConsistentCasingInFileNames": true, /* Garante que a capitalização está correta nas importações. */
    "strict": true, /* Ativa todas as opções de verificação de tipo estrita. */
    "skipLibCheck": true /* Pula a verificação de tipo de todos os arquivos .d.ts. */
  }
}